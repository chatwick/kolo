/* eslint-disable no-unused-vars */
export enum ORDER_PAYMENT_STATUS {
  PENDING = 0,
  PAID = 1,
  REJECTED = 2,
  CANCELLED = 3,
}

export enum QUERY_MUTATION_KEYS {
  USERS = "users",
  PRODUCTS = "products",
  INVENTORY = "inventory",
  ORDERS = "orders",
}
